import React from 'react';

export default function Product({title}) {
    if(loading){
        return <h2>Loading.......</h2>
    }
  return (
    <div>
     

    <div >{title}</div>

  

 
    
    </div>
  );
}
